version: '3.8'

services:
  web:
    build: .
    ports:
      - "8002:8002"
    volumes:
      - ./marketplace.db:/app/marketplace.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped